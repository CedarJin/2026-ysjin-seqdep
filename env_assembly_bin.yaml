# Conda environment for assembly and binning: MEGAHIT, MetaBAT2, CheckM2, GTDB-Tk
# Create with: conda env create -f env_assembly_bin.yaml
# Activate with: conda activate assemble

name: assemble
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - megahit
  - metabat2
  - checkm2
  - gtdbtk
  - bowtie2
  - samtools
